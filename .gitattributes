# Normalise line endings.
# Shell scripts MUST be LF: a CRLF gradlew or install.sh fails on Linux with
# "bad interpreter: No such file or directory".
* text=auto eol=lf

*.bat   text eol=crlf
gradlew text eol=lf
*.sh    text eol=lf

*.jar   binary
*.png   binary
