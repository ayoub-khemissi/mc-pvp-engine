// Pure Java. No Bukkit, no Paper, no database.
// All the real logic lives here so it can be unit-tested without a server.
// This is the TDD core of the engine.

dependencies {
    // (nothing — deliberately)
}
